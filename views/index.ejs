<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <h1>Hello <%= user ? user.profile.name : "Music Lover" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
  <% } %>
  <h2><%= title %></h2>
</main>

<%- include('./partials/footer') %>
